let fetchRes = fetch('http://worldtimeapi.org/api/timezone/Europe/London');fetchRes.then(res =>    res.json()).then(data => {    console.log('everything',data);    const utc = data.utc_datetime;    const date = new Date(utc);    const hour = date.getHours();    const minutes = date.getMinutes();    const dayofyear = data.day_of_year;    const dayofweek = data.day_of_week;    const weeknumber = data.week_number;    getDayofYear.innerHTML = dayofyear;    getDayofWeek.innerHTML = dayofweek;    getWeekNumber.innerHTML = weeknumber;    if(hour > 6 && hour < 16 ) {        document.body.classList.add('background-day');         getSrcImg.src = "img/assets/desktop/icon-sun.svg";         getDayNightCnt.innerHTML = 'GOOD MORNING, IT’S CURRENTLY';    }else {        document.body.classList.add('background-night');        getSrcImg.src = "img/assets/desktop/icon-moon.svg";        getDayNightCnt.innerHTML = 'GOOD EVENING, IT’S CURRENTLY';        moveBtmSect.classList.add('night-container');        getCurrentTime.setAttribute('style', 'color:white !important');        getLeftCont.setAttribute('style', 'border-right: 1px solid rgb(255,255,255, 0.25)')    }    if(hour < 10) {        getHours.innerHTML = `0${hour}`;    }else {        getHours.innerHTML = `${hour}`;    }    if(minutes < 10) {        getMinutes.innerHTML = `0${minutes}`;    }else {        getMinutes.innerHTML = `${minutes}`;    }    });//Get DOM Elementsconst moreLessBtn = document.querySelector('.more-less');const changeMoreTxt = document.querySelector('.more');const getArrowImg  = document.getElementById('arrow');const moveBtmSect = document.querySelector('.bottom-container');const moveMain = document.querySelectorAll('.move-up');const getHours = document.querySelector('.getHours');const getMinutes = document.querySelector('.getMinutes');const getDayofWeek = document.querySelector('.dayofweek');const getDayofYear = document.querySelector('.dayofyear');const getWeekNumber = document.querySelector('.weeknumber');const getSrcImg = document.getElementById('dayNight');const getDayNightCnt = document.querySelector('.night-day-phrase');const getCurrentTime = document.querySelector('.current-time');const getLeftCont = document.querySelector('.left-container');const toogleTheme = ()=> {    const checkMore = changeMoreTxt.innerHTML;    if(checkMore === 'more') {        changeMoreTxt.innerHTML = 'less';        getArrowImg.classList.add('rotateimg180');        moveBtmSect.setAttribute('style','transform: translateY(-16rem)');        for (let i = 0; i < moveMain.length; i++) {            moveMain[i].setAttribute('style','transform: translateY(-135%)')        }    }else {        changeMoreTxt.innerHTML = 'more';        getArrowImg.classList.remove('rotateimg180');        moveBtmSect.removeAttribute('style');        for (let i = 0; i < moveMain.length; i++) {            moveMain[i].removeAttribute('style')        }    }};//More-Less ButtonmoreLessBtn.addEventListener('click',()=> {    toogleTheme()});